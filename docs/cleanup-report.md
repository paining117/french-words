# 项目整理与上传记录（2026-09-23）

## 范围与保留

检查了根目录、页面、组件、服务、Repository、迁移、测试、数据构建、词库、文档、资源及 Git 上传范围。修改前对源文件制作 ZIP 备份，并对 125 个 TypeScript 文件做模块引用分析。

本次不改学习规则、复习规则、拼写规则、界面布局、数据 schema 或依赖版本。没有新增 migration，没有删除测试。清理前后比较证明 100 个受保护文件（业务服务、Repository、迁移、测试、内置词库与示例数据）逐字节一致。

## 精简内容

- 约 278.34 MiB 的旧 `.verification` 产物移到项目外备份，包含取消的远程方案、旧构建和日志。是归档，不宣称释放了同等磁盘空间。
- 删除 8 个无用／过时文件：无引用的旧内存搜索排序器、spacing、typography；过时的组件和 FSRS 占位 README；空背景目录说明；未配置使用的 splash-icon；不含实际配置的 `.env.example`。
- 学习与复习页分别合并两个互斥的「继续」按钮分支。答对延迟可跳过、错误反馈需手动继续、标熟详情可立即继续的行为均保留。
- 删除学习页两项无引用样式、首页多余 Fragment、被全局选项覆盖的重复导航配置和已取消 server 的 TypeScript 排除项。
- README 从 25,820 字节精简为 5,314 字节，统一为当前功能与使用方式；原文保存在项目外。历史 `docs/` 与真机检查清单保留。
- 保留完整生成词库及数据来源、许可、重建脚本；原始下载仅留本机。
- 本机 `.claude` 设置退出 Git 跟踪；忽略依赖、测试数据库、个人数据库、签名文件和安装包。
- 新增 iPhone 安装说明和最小 EAS `preview`／`production` 配置；未执行云构建或发布。

本机备份：`D:\同济大学\french-words-backup-20260923`，包含 `before-cleanup.zip`、`README-history.md` 和 `verification-history/`。

## 验证

| 项目 | 结果 |
| --- | --- |
| TypeScript 检查 | 通过 |
| 现有完整自动测试 | 123 / 123 通过 |
| 生产资源编译 | iOS、Android 均通过（JS/Hermes 和资源，非签名安装包） |
| 完整词库导入 | 通过，旧 Cards、日志、Sessions、轮次、签到及词书关系保留 |
| SQLite 完整性与外键 | 均为 ok |
| 本地查询验证 | 法语、重音／连字、中文等 12 组查询通过 |
| 一键启动器检查 | 能识别本机环境、自动选择端口；未重复启动现有服务 |
| SDK 依赖配套检查 | 离线检查通过，不等于联网核对所有最新版 |
| 上传文件常见密钥格式扫描 | 未发现匹配，不包含本机凭据或数据库 |

编译输出与日志在忽略的 `.verification/` 下。清理后没有做新的 iPhone 触摸、键盘或视觉验收；已有真机验收结论不能替代本次复验。

## 已知事项

- `npm audit` 返回 13 项 moderate、0 high、0 critical，来自现有 Expo／Router 传递依赖链；未执行会导致 Expo 主版本降级的自动修复。锁文件保持不变，依赖升级应另行验证。
- 设置很大的每轮词数时，保持单页总结会压缩字号；这是现有行为，本次未改变。
- 正式 iOS 安装尚需账号、设备注册和签名。GitHub 上的源码 ZIP 不能直接装进 iPhone。
- Expo Go 到独立 App 的本地学习记录不会自动迁移；本版本没有用户进度导入／导出界面。

## GitHub

目标为 `paining117/french-words` 私有仓库，保存应用代码、测试、离线词库、构建脚本和许可说明。个人学习数据库、原始下载、旧验证产物和本机凭据不上传。上传完成情况以任务最终回执及 Git 远端校验为准。
